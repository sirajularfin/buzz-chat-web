import Colors from '../../themes/colors';
import createStyles from '../../util/styles.util';

const styles = createStyles({
  container: {
    backgroundColor: Colors.PURPLE_A200,
    cursor: 'pointer',
    display: 'flex',
    justifyContent: 'center',
    paddingTop: '10px',
    paddingBottom: '10px',
    borderWidth: 0,
  },
});

export default styles;
